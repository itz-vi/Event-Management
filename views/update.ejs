<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>update</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<style>
    .container {
        background-color: rgba(28, 103, 122, 0.548);
        height: 300px;
        width: 500px;
        padding: 30px;
        font-size: 26px;
        font-weight: bold;
        border: 2px solid rgba(115, 115, 141, 0.566);
        border-radius: 10px;
        box-shadow: 0 4px 8px 6px rgba(20, 30, 213, 0.2), 3px 3px 2px 0 rgba(0, 0, 0, 0.10);
        color: rgba(19, 20, 23, 0.864);
    }
</style>

<body>
    <div class="container mt-5">
        <form action="/updateevent/<%= event._id %>" method="post">
            <div class="form-group">
                <label>Event</label>
                <input type="text" name="title" class="form-control" value="<%= event.title %>">
            </div>
            <div class="form-group">
                <label>Date</label>
                <input type="date" name="date" class="form-control" value="<%= event.date %>">
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-info">Submit</button>
            </div>
        </form>
    </div>
</body>

</html>